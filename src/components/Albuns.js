import React from 'react';

class Albuns extends React.Component {
  render() {
    // const { albuns } = this.props;
    return (
      <div>
        {/* {
           albuns !== undefined
           && <Route
             path="/album"
             render={
               (props) => <Album { ...props } album={ albuns } />
             }
           />
         } */}
      </div>
    );
  }
}

export default Albuns;
